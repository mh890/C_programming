#include <stdio.h>
#include <string.h>

/*
STRING is an array of char
can manipulate strings
    strcpy() -- copy a string over another string
    strcat() -- apprend a string to another string
    strcmp() -- compare two strings for equality
    strncmp() -- compare the first n charactsers of two strings
    strlen() -- calculate length of string

*/